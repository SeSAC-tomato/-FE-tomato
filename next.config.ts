import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // reactStrictMode: false,
  // allowedDevOrigins: ['172.16.15.71', 'localhost', '127.0.0.1', '192.168.0.2'],
  images: {
    domains: ["via.placeholder.com"],
  },
};

export default nextConfig;
